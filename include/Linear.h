#pragma once
#include <iostream>
#include <cmath>
#include <vector>

namespace WayFinder {
  struct lPoint {
    double x, y, rotation = 0, length = 0;
  };

  class Linear {
   public:
    
   private:
  };
}